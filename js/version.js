/*
 * EasyPen - app version (single source of truth).
 * Bump on every change to the app files: it renames the Service Worker
 * caches and is shown in the home screen footer.
 */
self.EASYPEN_VERSION = '9';
